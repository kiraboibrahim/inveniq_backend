upstream inveniq_backend_backend {
    server web:8000;
}

server {
    listen 80;

    location /media/ {
        alias /app/media/;
    }

    location /health {
        access_log off;
        return 200 "healthy\n";
    }

    location / {
        proxy_pass http://inveniq_backend_backend;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }
}
